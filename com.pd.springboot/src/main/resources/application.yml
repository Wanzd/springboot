spring:
    profiles:
        active: dev
    datasource:
        username: dev
        password: dev
        url: jdbc:mysql://localhost:3306/dev?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8
        driver-class-name: com.mysql.jdbc.Driver
    redis:
        database: 0
        host: localhost
        port: 6379
        password: 
        pool:
            max-active: 8
            max-wait: -1
            max-idle: 8
            min-idle: 0
        timeout: 0
       

server:
    port: 8080

mybatis:
    mapper-locations: classpath*:mapper/*.xml